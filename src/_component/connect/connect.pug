- var sectionTitle = data.connect.sectionTitle
include ../section-title/section-title

.column(span="row")
  .connect__avatar
.column(span="row")
.column(span="row")

each social in data.connect.socialList
  input.connect__radio(type="radio")
  .column(span="1")
    .connect__disc
      i(class=`icon__${social.icon}`)
