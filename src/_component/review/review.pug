- var sectionTitle = data['review'].sectionTitle
include ../section-title/section-title.pug

.column(span="row" height="10")
.column(span="row")
  each descriereM in data.review.descriereM
    .recomandare
      .recomandare-data
        .rec-imagine
          img(class="imagine" , src=`${descriereM.avatarPath}` , alt="")
        .descriereM-nume=descriereM.nameP
        .descriereM-data=descriereM.data
      .recomandare-descriere
         p=descriereM.descriereP
.column(span="row" height="10")
.column(span="row")
  button.buton__disc-dolarel
    i.icon__arrow-down
.column(span="row" height="10")
